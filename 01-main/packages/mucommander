DEFVER=1
get_github_releases "https://api.github.com/repos/mucommander/mucommander/releases/latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL="$(grep "browser_download_url.*${HOST_ARCH}.deb" "${CACHE_DIR}/${APP}.json" | head -n1 | cut -d'"' -f4)"
    VERSION_PUBLISHED="$(echo "${URL}" | cut -d'/' -f8)"
fi
PRETTY_NAME="MUCommander"
WEBSITE="https://github.com/mucommander/mucommander"
SUMMARY="muCommander is a lightweight, cross-platform file manager with a dual-pane interface."