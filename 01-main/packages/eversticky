DEFVER=1
get_github_releases "https://api.github.com/repos/itsmejoeeey/eversticky/releases/latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL="$(grep "browser_download_url.*.deb" "${CACHE_DIR}/${APP}.json" | head -n1 | cut -d'"' -f4)"
    VERSION_PUBLISHED="$(echo "${URL}" | cut -d'/' -f8 | tr -d v)"
fi
PRETTY_NAME="Eversticky"
WEBSITE="https://github.com/itsmejoeeey/eversticky"
SUMMARY="A Linux open-source sticky note client that syncs to Evernote."